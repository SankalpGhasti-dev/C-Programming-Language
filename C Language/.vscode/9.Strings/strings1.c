#include <stdio.h>

int main()
{

    char st[] = "abc";

    printf("%s\n", st);

    // This is an method to print string directly without use of for loop using %c.
    // It is method of printing string using %s.

    return 0;
}