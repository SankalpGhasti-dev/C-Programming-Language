#include <stdio.h>

int main()
{
    int length = 3;
    int breadth = 4;

    printf("The area of rectangle is %d", length * breadth);

    // here are we directly declare the data .. so that we dont need you use scanf function ...

    return 0;
}
